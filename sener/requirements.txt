# Python >= 3.8 is required.
# torch 2.0+ and transformers 4.x do not support Python 3.7.

# Install torch separately following https://pytorch.org/get-started/locally/
# so that the correct build (CUDA / MPS / CPU) is selected automatically.
# Minimum required version: 2.0 (for scatter_reduce_ MPS support)
torch>=2.0.0

# Core ML libraries
transformers>=4.30.0
# peft >= 0.3.0 is required for LoraConfig with bias="lora_only" support
peft>=0.3.0
accelerate>=0.20.3

# Tokenizer / serialisation
sentencepiece
safetensors

# Scientific computing
numpy

# Utilities
tqdm
